{% extends 'base.html.twig' %}

{% block title %}SystemElects index{% endblock %}

{% block body %}
    <h1>SystemElects index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Libelle</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for system_elect in system_elects %}
            <tr>
                <td>{{ system_elect.id }}</td>
                <td>{{ system_elect.Libelle }}</td>
                <td>
                    <a href="{{ path('system_elects_show', {'id': system_elect.id}) }}">show</a>
                    <a href="{{ path('system_elects_edit', {'id': system_elect.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('system_elects_new') }}">Create new</a>
{% endblock %}