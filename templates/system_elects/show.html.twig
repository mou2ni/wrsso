{% extends 'base.html.twig' %}

{% block title %}SystemElects{% endblock %}

{% block body %}
    <h1>Systeme de transfert mobile</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ system_elect.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ system_elect.Libelle }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('system_elects_index') }}">Retour à la liste</a>

    <a href="{{ path('system_elects_edit', {'id': system_elect.id}) }}">Modifier</a>

    {{ include('system_elects/_delete_form.html.twig') }}
{% endblock %}