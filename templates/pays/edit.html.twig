{% extends 'base.html.twig' %}

{% block title %}Edit Pays{% endblock %}

{% block body %}
    <h1>Modifier Pays | Ajouter : <a href="{{ path('pays_new') }}"> Un Pays</a> | <a href="{{ path('zones_new') }}"> Une Zones</a></h1>

    {{ include('pays/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

    <div class="form-inline"><a href="{{ path('pays_index') }}">Retour à la liste</a> ||

        <a href="{{ path('pays_edit', {'id': pay.id}) }}">Modifier</a> ||

        {{ include('pays/_delete_form.html.twig') }}
    </div>
    {{ include('pays/_liste.html.twig') }}
{% endblock %}