<div class="well">
{{ form_start(form, {'attr': {'class': 'form-horizontal'}}) }}
    {{ form_row(form.dateRecu) }}
    {{ form_row(form.nom) }}
    {{ form_row(form.prenom) }}
    {{ form_row(form.typePiece) }}
    {{ form_row(form.numPiece) }}
    {{ form_row(form.expireLe) }}
    {{ form_row(form.paysPiece) }}
    {{ form_row(form.motif) }}

<h3>Devises</h3>

    {{ include('devise_mouvements/_inline_form.html.twig') }}

    <div class="form-group">
    <div class="form-row">
    {{ form_widget(form.save_and_new) }}{{ form_widget(form.save_and_print) }}{{ form_widget(form.reset) }}{{ form_widget(form.close) }}

    </div>
    </div>

    {{ form_end(form) }}
</div>