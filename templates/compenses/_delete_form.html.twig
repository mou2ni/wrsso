<form method="post" action="{{ path('compenses_delete', {'id': compense.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ compense.id) }}">
    <button class="btn">Supprimer</button>
</form>
