<form method="post" action="{{ path('transaction_comptes_delete', {'id': transaction_compte.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ transaction_compte.id) }}">
    <button class="btn">Delete</button>
</form>