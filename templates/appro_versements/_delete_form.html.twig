<form method="post" action="{{ path('appro_versements_delete', {'id': appro_versement.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ appro_versement.id) }}">
    <button class="btn">Supprimer</button>
</form>
