<form class="form-inline" method="POST" action="{{ (journeeCaisse.statut != 'X')?path('electroniques_ajout', {'operation': operation}):path('system_elect_inventaires_show', {'id':journeeCaisse.systemElectInventFerm.id, 'operation': operation})}}" >
    <input type="hidden" name="_method" value="UPDATE">
    {#<input type="hidden" name="_operation" value="{{ operation }}">#}
    <input type="hidden" name="_token" value="{{ csrf_token('elect' ~ id)}}">
    <input type="hidden" name="_journeeCaisse" value="{{ journeeCaisse.id }}">
    <button class="{{ btn_class|default('btn-info') }}"  name={{ btn_name }}>{{ btn_label }}</button>
</form>