<form method="post" action="{{ path('devise_mouvements_delete', {'id': devise_mouvement.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ devise_mouvement.id) }}">
    <button class="btn">Delete</button>
</form>