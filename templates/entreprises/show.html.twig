{% extends 'base.html.twig' %}

{% block title %}Entreprises{% endblock %}

{% block body %}
    <h1>Entreprises |  <a href="{{ path('entreprises_index') }}">Listing</a> |

        <a href="{{ path('entreprises_edit', {'id': entreprise.id}) }}">Modifier</a> </h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entreprise.id }}</td>
            </tr>
            <tr>
                <th>Code</th>
                <td>{{ entreprise.code }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ entreprise.libelle }}</td>
            </tr>
            <tr>
                <th>Adresse</th>
                <td>{{ entreprise.adresse }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ entreprise.representant }}</td>
            </tr>
        </tbody>
    </table>

    {{ include('entreprises/_delete_form.html.twig') }}
{% endblock %}
