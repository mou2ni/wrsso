<form method="post" action="{{ path('rubrique_analyses_delete', {'id': rubrique_analysis.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ rubrique_analysis.id) }}">
    <button class="btn">Delete</button>
</form>
