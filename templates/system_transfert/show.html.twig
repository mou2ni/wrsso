{% extends 'base.html.twig' %}

{% block title %}SystemTransfert{% endblock %}

{% block body %}
    <h1>Detail du système de transfert</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ system_transfert.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ system_transfert.libelle }}</td>
            </tr>
        </tbody>
    </table>

    <div class="form-inline"><a href="{{ path('system_transfert_index') }}">Retour à la liste</a> ||

    <a href="{{ path('system_transfert_edit', {'id': system_transfert.id}) }}">Modifier</a> ||

    {{ include('system_transfert/_delete_form.html.twig') }}
    </div>
    {{ include('system_transfert/_liste.html.twig') }}

{% endblock %}