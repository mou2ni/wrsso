<form method="post" action="{{ path('system_transfert_delete', {'id': system_transfert.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ system_transfert.id) }}">
    <button class="btn">Delete</button>
</form>