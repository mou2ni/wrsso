<form method="post" action="{{ path('billetage_lignes_delete', {'id': billetage_ligne.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ billetage_ligne.id) }}">
    <button class="btn">Delete</button>
</form>