{% extends 'base.html.twig' %}

{% block title %}SystemElectLigneInventaires index{% endblock %}

{% block body %}
    <h1>SystemElectLigneInventaires index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Solde</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for system_elect_ligne_inventaire in system_elect_ligne_inventaires %}
            <tr>
                <td>{{ system_elect_ligne_inventaire.id }}</td>
                <td>{{ system_elect_ligne_inventaire.solde }}</td>
                <td>
                    <a href="{{ path('system_elect_ligne_inventaires_show', {'id': system_elect_ligne_inventaire.id}) }}">show</a>
                    <a href="{{ path('system_elect_ligne_inventaires_edit', {'id': system_elect_ligne_inventaire.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('system_elect_ligne_inventaires_new') }}">Create new</a>
{% endblock %}