{% extends 'base.html.twig' %}

{% block title %}Billets{% endblock %}

{% block body %}
    <h1>Billets</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ billet.id }}</td>
            </tr>
            <tr>
                <th>Valeur</th>
                <td>{{ billet.valeur }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('billets_index') }}">back to list</a>

    <a href="{{ path('billets_edit', {'id': billet.id}) }}">edit</a>

    {{ include('billets/_delete_form.html.twig') }}
{% endblock %}