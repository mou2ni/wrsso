{% extends 'base.html.twig' %}

{% block title %}Caisses{% endblock %}

{% block body %}
    <h1>Details Caisses</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ caiss.id }}</td>
            </tr>
            <tr>
                <th>Code</th>
                <td>{{ caiss.code }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ caiss.libelle }}</td>
            </tr>
            <tr>
                <th>Compte opération</th>
                <td>{{ caiss.compteOperation }}</td>
            </tr>
            <tr>
                <th>Statut</th>
                <td>{{ caiss.statut }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('caisses_index') }}">Liste des caisses</a> |

    <a href="{{ path('caisses_edit', {'id': caiss.id}) }}">Modifier</a> |

    <a href="{{ path('caisses_nouvelle_journee', {'id': caiss.id}) }}">Ouvrir nouvelle journée</a>

    {{ include('caisses/_delete_form.html.twig') }}
{% endblock %}