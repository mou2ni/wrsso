{% extends 'base.html.twig' %}

{% block title %}Caisses{% endblock %}

{% block body %}
    <h1>Caisses</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ caiss.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ caiss.libelle }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('caisses_index') }}">back to list</a>

    <a href="{{ path('caisses_edit', {'id': caiss.id}) }}">edit</a>

    {{ include('caisses/_delete_form.html.twig') }}
{% endblock %}