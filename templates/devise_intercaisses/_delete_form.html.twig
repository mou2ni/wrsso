<form method="post" action="{{ path('devise_intercaisses_delete', {'id': devise_intercaiss.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ devise_intercaiss.id) }}">
    <button class="btn">Delete</button>
</form>