{% extends 'base.html.twig' %}

{% block title %}Comptes{% endblock %}

{% block body %}
    <h1>Comptes</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ compte.id }}</td>
            </tr>
            <tr>
                <th>NumCompte</th>
                <td>{{ compte.numCompte }}</td>
            </tr>
            <tr>
                <th>Intitule</th>
                <td>{{ compte.intitule }}</td>
            </tr>
            <tr>
                <th>SoldeCourant</th>
                <td>{{ compte.soldeCourant }}</td>
            </tr>
            <tr>
                <th>TypeCompte</th>
                <td>{{ compte.typeCompte }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('comptes_index') }}">back to list</a>

    <a href="{{ path('comptes_edit', {'id': compte.id}) }}">edit</a>

    {{ include('comptes/_delete_form.html.twig') }}
{% endblock %}