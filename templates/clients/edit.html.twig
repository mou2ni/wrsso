{% extends 'base.html.twig' %}
{% block title %}Modifier Clients{% endblock %}

{% block body %}
    <h1>Mdifier un Client</h1>

    {{ include('clients/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

    <div class="form-inline"><a href="{{ path('clients_index') }}">Retour à la liste</a>||

        <a href="{{ path('clients_edit', {'id': client.id}) }}">Modifier</a>||

        {{ include('clients/_delete_form.html.twig') }}
    </div>
    {{ include('clients/_liste.html.twig') }}
{% endblock %}