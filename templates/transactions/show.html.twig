{% extends 'base.html.twig' %}

{% block title %}Transactions{% endblock %}

{% block body %}
    <h1>Transactions</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ transaction.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ transaction.libelle }}</td>
            </tr>
            <tr>
                <th>DateTransaction</th>
                <td>{{ transaction.dateTransaction ? transaction.dateTransaction|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('transactions_index') }}">back to list</a>

    <a href="{{ path('transactions_edit', {'id': transaction.id}) }}">edit</a>

    {{ include('transactions/_delete_form.html.twig') }}
{% endblock %}