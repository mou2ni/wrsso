{% extends 'base.html.twig' %}

{% block title %}RecetteDepenseAgences{% endblock %}

{% block body %}
    <h1>RecetteDepenseAgences</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ recette_depense_agence.id }}</td>
            </tr>
            <tr>
                <th>MPart</th>
                <td>{{ recette_depense_agence.mPart }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('recette_depense_agences_index') }}">back to list</a>

    <a href="{{ path('recette_depense_agences_edit', {'id': recette_depense_agence.id}) }}">edit</a>

    {{ include('recette_depense_agences/_delete_form.html.twig') }}
{% endblock %}
