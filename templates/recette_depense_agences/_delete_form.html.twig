<form method="post" action="{{ path('recette_depense_agences_delete', {'id': recette_depense_agence.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ recette_depense_agence.id) }}">
    <button class="btn">Delete</button>
</form>
