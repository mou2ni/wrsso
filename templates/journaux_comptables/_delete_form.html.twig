<form method="post" action="{{ path('journaux_comptables_delete', {'id': journaux_comptable.id}) }}" onsubmit="return confirm('Confirmer la suppression ?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ journaux_comptable.id) }}">
    <button class="btn">Supprimer</button>
</form>
