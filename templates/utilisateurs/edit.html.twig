{% extends 'base.html.twig' %}

{% block title %}Edit Utilisateurs{% endblock %}

{% block body %}
    {% if is_granted('ROLE_ADMIN') %}
    <h1>Mdifier Utilisateurs</h1>

    {{ include('utilisateurs/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

    <a href="{{ path('utilisateurs_index') }}">Retour à la liste</a>

    {{ include('utilisateurs/_delete_form.html.twig') }}
        {% else %}
        <h1>Vous ne pouvez pas modifier d'utilisateur</h1>
    {% endif %}
{% endblock %}