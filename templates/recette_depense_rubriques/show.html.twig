{% extends 'base.html.twig' %}

{% block title %}RecetteDepenseRubriques{% endblock %}

{% block body %}
    <h1>RecetteDepenseRubriques</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ recette_depense_rubrique.id }}</td>
            </tr>
            <tr>
                <th>MPart</th>
                <td>{{ recette_depense_rubrique.mPart }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('recette_depense_rubriques_index') }}">back to list</a>

    <a href="{{ path('recette_depense_rubriques_edit', {'id': recette_depense_rubrique.id}) }}">edit</a>

    {{ include('recette_depense_rubriques/_delete_form.html.twig') }}
{% endblock %}
